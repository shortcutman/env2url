{
  "name": "env2url",
  "version": "1.0.0",
  "description": "Take env vars and send them as parameters to a url",
  "main": "env2url.js",
  "scripts": {
    "start": "NODE_ENV=production node env2url.js",
    "debug": "node env2url.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "author": "Daniel Burke",
  "license": "ISC"
}
